package itfmod.itf;

/**
 * Built to customize the order of items in a CreativeTab.
 */
public interface OrderedItem
{
	/**
	 * Returns an ID with a custom order
	 *
	 * @return an ID with a custom order
	 */
	int getOrderId();
}
